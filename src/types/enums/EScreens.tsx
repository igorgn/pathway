// We dont use naming convention to add E in front of enum, or I infront of interface. Names should be simple and human readable, so Screens are fully sufficient and clear. Same with the file name.
export enum EScreens {
  Main = 'Main',
  AddActivity = 'AddActivity',
}
